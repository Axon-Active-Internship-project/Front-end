export enum HeaderOptions {
  total = "x-wp-total",
  totalPages = "x-wp-totalpages",
}

export enum ErrorInputMessage {
  tooLong = "Too many characters, Enter major again",
  tooFewer = "Searching keyword should be more than 1 letter",
  htmlTag = "Invalid searching keyword",
  specailKey = "Allowed characters a-z, A-Z, 0-9",
}
